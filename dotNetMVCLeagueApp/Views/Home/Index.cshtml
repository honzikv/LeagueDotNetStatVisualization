@model dotNetMVCLeagueApp.Data.FrontendModels.Home.HomePageViewModel
@{
    ViewData["Title"] = "Search";
}

<div class="container">
    <div class="row align-items-center">
        <div class="col-lg-4 mx-auto">

            <div class="mt-4 p-4 card-search">

                <h1 class="heading-search text-center">Search2</h1>
                <form asp-controller="Summoner" asp-action="SummonerInfo">
                    <select class="form-control mb-1" name="server">
                        @foreach (var (queryKey, serverName) in Model.ServerList) {
                            <option name="@queryKey">@serverName</option>
                        }
                    </select>

                    <input type="text" class="form-control mb-1" id="name" placeholder="Caps" name="name"/>

                    <button type="submit" class="btn btn-primary float-end">
                        <i class="fa fa-search"></i>
                    </button>

                </form>

            </div>
        </div>
    </div>


</div>